FROM node:22-bookworm-slim

# Install OpenClaw Studio at build time (not runtime)
RUN npm install -g openclaw-studio@latest

VOLUME /root/.openclaw

EXPOSE 3000

CMD ["openclaw-studio", "--run", "--port", "3000"]
